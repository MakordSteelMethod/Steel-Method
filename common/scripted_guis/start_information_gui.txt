scripted_gui = {

	start_info_window_gui = {
	
		context_type = player_context
		window_name = "start_info_window"
		
		visible = {
			check_variable = { start_info_visible = 1 }
		}
		
		effects = {
			
			close_click = { set_variable = { start_info_visible = 0 } }
			
		}
		
		triggers = {
		
			
			basic_picture_visible = { NOT = { tag = DAT } }
			DAT_picture_visible = {	tag = DAT }
			
		}
		
	}
	
}